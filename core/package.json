{
  "name": "@mi/core",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "files": [
    "build/"
  ],
  "scripts": {
    "build": "yarn clean & tsc -p --declarationDir dist & rollup -c rollup.config.js",
    "testt": "tsc --outDir dist/lib --declarationDir dist/types --declaration true && rollup -c",
    "tsc": "tsc -b",
    "clean": "rimraf build",
    "test": "echo \"Error: no test specified\" && exit 1 tsc -p tsconfig.json --declarationDir build "
  },
  "author": "",
  "license": "ISC",
  "type": "module"
}
